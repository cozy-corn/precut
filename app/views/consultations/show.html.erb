<div class="max-w-3xl mx-auto p-6 bg-white shadow-lg rounded-lg my-8">
  <h1 class="text-3xl font-bold mb-6 text-center text-gray-800 border-b pb-2">
    カルテ (カウンセリング結果)
  </h1>

  <div class="mb-8">
    <p class="text-lg text-gray-600">
      <span class="font-semibold">カルテID:</span> <%= @consultation.id %>
    </p>
    <p class="text-lg text-gray-600">
      <span class="font-semibold">開始日時:</span> <%= @consultation.created_at.strftime("%Y年%m月%d日") %>
    </p>
  </div>
  
  <hr>

  <h2 class="text-2xl font-semibold mt-6 mb-4 text-gray-700">カウンセリング履歴</h2>

  <div class="space-y-6">
    <% @answers.each.with_index(1) do |answer, index| %>
      <div class="border-l-4 border-blue-500 pl-4 py-2 bg-gray-50 rounded-r-md">
        
        <p class="text-base font-bold text-gray-900 mb-1">
          Q<%= index %>: <%= answer.question %>
        </p>

        <div class="p-3 bg-white border border-gray-200 rounded-md shadow-sm">
          <p class="text-gray-700 whitespace-pre-wrap"><%= answer.answer %></p>
        </div>
      </div>
    <% end %>
  </div>

<%# ========================================================== %>
  <%# ここからQRコード/共有機能のエリア %>
  <h2 class="text-2xl font-semibold mt-10 mb-4 text-center text-gray-700 border-t pt-6">
    共有リンクでカルテを美容師さんに送信しよう
  </h2>
  
  <%# 部分テンプレートの呼び出し (Turbo Frameで囲む %>
  <turbo-frame id="sharing_widget">
    <%= render 'consultations/sharing_widget', consultation: @consultation %>
  </turbo-frame>
  <%# ========================================================== %>

  <div class="mt-10 text-center">
    <%= link_to 'ホームに戻る', root_path, class: "text-blue-500 hover:underline" %>
  </div>
</div>
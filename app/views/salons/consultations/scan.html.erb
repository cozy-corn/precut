<div class="p-4 sm:p-6 lg:p-8 bg-gray-50 min-h-screen">
  <div class="max-w-xl mx-auto bg-white shadow-2xl rounded-2xl p-6 sm:p-8 text-center">
    <h1 class="text-3xl font-extrabold text-gray-900 mb-6 border-b pb-3">
      カルテ用 QR スキャナー
    </h1>

    <p class="text-gray-600 mb-6">カメラにQRコードをかざしてください。</p>

    <!-- カメラ映像を表示する領域 (ID: "reader" はJSで参照されます) -->
    <div 
      id="reader" 
      class="w-full aspect-square max-w-sm mx-auto overflow-hidden border-4 border-indigo-500 rounded-xl shadow-inner" 
      style="min-height: 250px;"
    >
    </div>

    <!-- 読み取り結果を表示する場所 -->
    <div id="result" class="mt-6 p-4 bg-gray-100 text-gray-800 font-semibold rounded-lg hidden">
      読み取り結果がここに表示されます。
    </div>

    <!-- エラーメッセージを表示する場所 -->
    <div id="error" class="mt-6 p-4 bg-red-100 text-red-800 font-semibold rounded-lg hidden">
      エラーメッセージがここに表示されます。
    </div>
  </div>
</div>


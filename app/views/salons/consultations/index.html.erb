<div class="h-full w-full bg-gray-50 flex flex-col">
  <%# 画面タイトル %>
  <h1 class="text-3xl font-bold mb-6 text-center text-gray-800">
    顧客カルテ一覧
  </h1>
  <%# ページのメインコンテンツ部分（未回答カルテ一覧の部分全体のスタート） - 固定ヘッダーの高さ分、上部に余白を設けています　ここをオートコンプリート機能を入れる予定 %>
  <main class="container mx-auto max-w-xl w-full p-0 pt-10 pb-4">
    <%# 1. 検索フォーム（独立したセクションとして集計ヘッダーの上に配置） %>
    <div class="p-4 bg-white shadow-md border-b border-gray-100">
      <%# _search.html.erb をここにレンダリング %>
      <div class="text-base m-8">
        <%= render 'search', q: @q, url: salons_consultations_path %>
      </div>
    </div>
    <%# 集計数値 %>
    <div class="bg-emerald-500 text-white px-4 pb-4 flex justify-between text-center">
      <div class="flex-1">
        <div class="text-3xl font-extrabold"><%= @total_consultations_count %></div>
        <div class="text-xs">全てのカルテ</div>
      </div>
      <div class="flex-1 border-l border-emerald-400">
        <div class="text-3xl font-extrabold"><%= @archived_consultations_count %></div>
        <div class="text-xs">確認済</div>
      </div>
    </div>
    <%# 4. 顧客カルテのリスト %>
    <div class="p-4 space-y-3">
      <%# カルテが空かどうかのチェック %>
      <% if @consultations.empty? %>
        <p class="text-center text-gray-500 mt-12 p-8 bg-white rounded-xl shadow-inner border border-gray-100">
          まだ紐付けられたカルテ履歴がありません。
        </p>
      <% else %>
        <% @consultations.each_with_index do |consultation, index| %>
          <%# Q4のAnswerを検索 %>
          <% q4_question_text = "ご希望のヘアスタイルを教えてください。" %>
          <% q4_answer = consultation.answers.find { |a| a.question == q4_question_text } %>
          <a href="<%= salons_consultation_path(consultation) %>" class="block bg-white p-4 rounded-xl shadow-md hover:shadow-lg transition duration-300 border border-gray-200 flex items-start space-x-4">
            <%# ユーザーアバター（写真のデザインを再現） %>
            <div class="flex-shrink-0 mt-1">
              <div class="w-10 h-10 bg-indigo-100 text-indigo-600 rounded-full flex items-center justify-center font-bold text-lg">
                <%= consultation.user.full_name.to_s.slice(0) if consultation.user.full_name.present? %>
              </div>
            </div>
            <div class="flex-grow">
              <%# 顧客名と未読/既読ステータス全体 %>
              <div class="flex justify-between items-center mb-1">
                <%# 顧客名（EX「山田太郎様」の部分） %>
                <div class="text-lg font-bold text-gray-800">
                  <%= consultation.user.full_name %> 様
                </div>
                <!-- 日付 -->
                <span class="text-sm text-gray-500">
                  <%= consultation.created_at.strftime("%Y年%-m月%-d日") %>
                </span>
              </div>
              <%# Q4の質問と回答（figmaの「ショートボブ希望、癖毛対応...」の部分） %>
              <% if q4_answer.present? %>
                <p class="text-sm text-gray-600 line-clamp-2">
                  <span class="font-medium text-gray-700">
                    <%= q4_answer.question %>:
                  </span>
                  <%= q4_answer.answer %>
                </p>
              <% else %>
                <p class="text-sm text-gray-400 italic">
                  回答が見つかりません
                </p>
              <% end %>
            </div>
            <%# 矢印アイコン %>
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="w-5 h-5 text-gray-400 flex-shrink-0 mt-1">
              <path d="M9 18l6-6-6-6"></path>
            </svg>
          </a>
        <% end %>
      <% end %>
    </div>
  </main>
</div>
